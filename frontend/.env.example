# Optional. Defaults to `/api` (Vite dev server proxies to backend; Docker build uses the same).
# Set only if the API is on another origin, e.g.:
# VITE_API_BASE_URL=http://localhost:5000

VITE_API_BASE_URL=/api
